/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "srv666826.hstgr.cloud",
      "localhost",
      "images.unsplash.com",
      "hire.mn",
    ],
  },
};

export default nextConfig;
